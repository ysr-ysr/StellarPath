# StellarPath Backend

Starter Node.js/Express backend for the StellarPath AI-powered career management platform.

## Setup

1. Install dependencies:

```bash
npm install
```

2. Update `.env` with your PostgreSQL credentials.

3. Start the development server:

```bash
npm run dev
```

The API runs at `http://localhost:5000`.
